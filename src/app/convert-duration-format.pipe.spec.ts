import { ConvertDurationFormatPipe } from './convert-duration-format.pipe';

describe('ConvertDurationFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertDurationFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
